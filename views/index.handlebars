<h1>Burgers!</h1>

<h2>Burgers that are ready!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoure}}
      {{> burger-block devoure=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoure!</h2>

<ul>
  {{#each burgers}}
    {{#if devoure}}
      {{> burger-block devoure=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    {{!-- <label for="sleepy">Hungry?</label><br> --}}
    {{!-- <input type="radio" name="sleepy" value="1" checked> Sleepy!<br> --}}
    <input type="radio" name="devoure" value="0"> Ready
  </div>

  <button type="submit">Add Burger</button>
</form>
